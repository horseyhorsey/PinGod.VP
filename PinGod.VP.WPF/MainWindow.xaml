<Window x:Class="PinGod.VP.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PinGod.VP.WPF"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="PinGod.VP Controller Setup" Height="369" Width="369">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="9"/>
            <Setter Property="BorderThickness" Value="5"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Background="LightGray" x:Name="MainGrid">
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Click="InstallButton_Click">INSTALL</Button>
            <Button Click="RemoveButton_Click">REMOVE</Button>
        </StackPanel>
    </Grid>
</Window>
